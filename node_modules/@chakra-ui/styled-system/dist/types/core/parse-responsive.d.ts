import { Dict } from "@chakra-ui/utils";
import { StyleFunction } from "./types";
interface Props {
    theme?: Dict;
    [k: string]: any;
}
export declare const createMediaQuery: (n: string) => string;
declare type ValueType = string | number | ((theme: Dict) => string | number);
interface ParseResponsiveArrayOptions {
    mediaQueries: string[];
    styleFunction: StyleFunction;
    scale: any;
    value: Array<ValueType>;
    props: Props;
}
export declare function parseResponsiveArray(options: ParseResponsiveArrayOptions): Record<string, any>;
interface ParseResponsiveObjectOptions {
    breakpoints: Dict;
    styleFunction: StyleFunction;
    scale: any;
    value: Record<string, ValueType>;
    props: Props;
}
export declare function parseResponsiveObject(options: ParseResponsiveObjectOptions): Record<string, any>;
export {};
