export interface BaseBreakpointConfig extends Record<string, string> {
    sm: string;
    md: string;
    lg: string;
    xl: string;
}
export declare type Breakpoints<T = BaseBreakpointConfig> = string[] & WithBase<T>;
export declare type WithBase<T> = T & {
    base: "0em";
};
export declare const createBreakpoints: <T extends BaseBreakpointConfig>(config: T) => Breakpoints<T>;
