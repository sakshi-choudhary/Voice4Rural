export default function createMediaQueries(breakpoints: string[]): MediaQuery[];
interface MediaQuery {
    breakpoint: string;
    maxWidth?: string;
    minWidth: string;
    query: string;
}
export {};
